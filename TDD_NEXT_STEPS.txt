BLOCKER
- pytest is not installed.
- `python3 -m venv` fails because python3-venv is missing.
- `apt install` is blocked by permissions.
- `uv pip install --system pytest` is blocked by PEP 668.
- `pip install --user pytest` returned `pip: command not found`.

ACTION REQUIRED FROM USER
1) Run WSL as Administrator once, or install WSL properly with sudo rights.
2) Then run:
   sudo apt update && sudo apt install -y python3.12-venv
   python3 -m venv /mnt/c/Users/LocalAdmin/diskstat/.venv
   /mnt/c/Users/LocalAdmin/diskstat/.venv/bin/pip install pytest

TDD PLAN FOR DISKSTAT (after pytest is available)
- Tests go in tests/test_diskstat.py
- Use a small temp dir fixture instead of mocking os.scandir
- First tests:
  1) test_normalize_path_windows_drive
  2) test_normalize_path_wsl_path_passthrough
  3) test_ext_category_file_mappings
  4) test_format_bytes_units
  5) test_scan_returns_tree_and_stats
  6) test_build_flat_limits_nodes
  7) test_render_html_writes_files
